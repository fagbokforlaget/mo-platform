image: node:12

pipelines:
  default:
    - step:
        script: # Modify the commands below to build your repository.
          - npm --version
          - node --version
          - npm install -g lerna
          - lerna bootstrap
          - cd packages/@mo-platform/auth && npm i && cd ../../../
          - npm test
